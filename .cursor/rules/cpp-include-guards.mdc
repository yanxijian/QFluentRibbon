---
description: C++ header include-guard style __TARGET_FILE_H__
globs: "**/*.{h,hpp,hxx,hh}"
alwaysApply: false
---

# Include guards

Do **not** use `#pragma once`. Use:

```text
__{TARGET}_{FILENAME}_H__
```

- **TARGET**: 产品短前缀 **`QFR`**（与现有头一致；对应库目标 `qfluentribbon`）
- **FILENAME**: 文件名 stem 大写（`ribbon_bar.hpp` → `RIBBON_BAR`）；同目标下重名时加父目录（如 `LAYOUT_COLLAPSE`）
- 后缀统一 `_H__`（含 `.hpp`）
- 形如 `__…_H__`（双下划线前缀/后缀）为本仓库约定

## Target map (headers)

| Path | Target macro |
|------|----------------|
| `include/qfluentribbon/**` | `QFR` |
| `examples/**`（若有公共头） | `QFR_GALLERY` |

## Examples

```cpp
// include/qfluentribbon/ribbon_bar.hpp
#ifndef __QFR_RIBBON_BAR_H__
#define __QFR_RIBBON_BAR_H__
// ...
#endif  // __QFR_RIBBON_BAR_H__

// include/qfluentribbon/layout/collapse.hpp
#ifndef __QFR_LAYOUT_COLLAPSE_H__
#define __QFR_LAYOUT_COLLAPSE_H__
```

## Enforcement

Not clang-format. Follow this Cursor rule when editing headers.
