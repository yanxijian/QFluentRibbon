# Source layout

Implementation arrives with milestones in [docs/zh/dev-plan.md](../docs/zh/dev-plan.md).

Planned:

- `layout/` — collapse / sizing rules (unit-testable)
- `chrome/` — shell painting helpers (ThemeStore-backed)
- Widgets wiring for `RibbonBar` / `RibbonTab` / `RibbonGroup` / …
